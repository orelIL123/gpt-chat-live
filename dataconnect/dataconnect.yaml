specVersion: "v1"
serviceId: "chatbotend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chatbotend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
